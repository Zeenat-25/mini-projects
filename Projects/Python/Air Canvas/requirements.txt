# Air Canvas - Python Dependencies
# Install with: pip install -r requirements.txt

# Core vision + hand tracking
opencv-python>=4.8.0
mediapipe>=0.10.0

# 3D rendering (optional but recommended)
PyOpenGL>=3.1.7
PyOpenGL_accelerate>=3.1.7   # Compiled bindings for speed

# Numerical computing (usually already installed)
numpy>=1.24.0
